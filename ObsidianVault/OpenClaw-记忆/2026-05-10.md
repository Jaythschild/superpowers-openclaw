# 2026-05-10 Daily Memory

## 生图方式偏好（21:35）
- 杰斯让我学会 OpenClaw 控制 UI 的生图方式：使用 image_generation JSON 结构记录 model/prompt/size/generated_at/output_path/media_reference。
- 示例模型：`openai/gpt-image-2`；示例尺寸：`1024x1024`；输出路径形如 `D:\OpenClawWindows\home\.openclaw\media\tool-image-generation\image-*.png`，回传用 `MEDIA:<path>` 或消息工具 media/path/filePath。
- 后续生成图片时，优先用这种明确参数 + 可回传媒体引用的方式组织结果。

## 生图测试（22:03）
- 杰斯让我生成了一张测试图片："一只小小的赛博朋克斑鸠，霓虹蓝粉色，3D卡通风格，白色背景"。
- 图片已成功生成并回传，路径：`D:\openclaw-stack\state\media\tool-image-generation\image-1---a7356fc9-b623-4d52-95d5-a81728a52ac6.png`。
- 本次测试验证了图像生成功能正常运行。

## 生图 Skill 制作（22:08）
- 杰斯让我把生图方式制作成 skill，写入 `D:\openclaw-stack\workspace\skills\image-generation\SKILL.md`。
- Skill 内容包含：触发条件、三步生成流程（解析意图 → 调用 image_generate → 结构化回传）、输出模板、JSON 元数据格式、Prompt 编写指南、模型配置（固定 openai/gpt-image-2）、严禁行为。
- 后续用户要求生成图片时，会自动匹配此 skill 并按流程执行。

## 图片统一存储文件夹（22:09）
- 杰斯要求所有生成的图片统一放在 `D:\openclaw-stack\workspace\images\` 文件夹中，不再散落在 workspace 根目录或 tool-image-generation 目录。
- 已创建 `images/` 文件夹，并将之前生成的两张图片移入（赛博朋克斑鸠 + 欧洲公主肖像）。
- 同时更新了 image-generation skill 中的输出路径，后续生成的图片默认写入 `images/`。

## 图片双通道回传规则（22:19）
- 杰斯明确要求：图片生成后**同时**保存到 `images/` 文件夹 **且** 在对话中通过 MEDIA 标签回传预览。
- 文件夹 = 持久存档，对话 = 即时预览，两个通道缺一不可。
- 已更新 image-generation skill，加入"双通道"输出模板和严禁行为。

## 图片分辨率与比例可设置（22:20）
- 杰斯询问图片分辨率和比例能否设置，我确认支持 `size`、`aspectRatio`、`resolution`、`outputFormat` 等参数。
- `openai/gpt-image-2` 对 `aspectRatio` 参数支持有限，最稳妥的方式是用 `size` 直接指定宽高（如 `1920x1080` 表示 16:9）。

## 龙虾徽章测试（22:22）
- 杰斯让我生成"一枚迷你红色龙虾徽章，3D卡通风格，白色背景"。
- 图片已成功生成并双通道回传，路径：`D:\openclaw-stack\workspace\images\image-1---62440636-3ff2-4504-9836-0106e3fcd64e.png`。

## 每日 GitHub 同步（22:32）
- 通过 cron 触发了每日 GitHub 同步
- 分支：main
- 提交：`0e74051` - "Auto-sync: 2026-05-10"
- 变更：14 个文件，41 行新增
  - 修改：`.gitignore`、`AGENTS.md`
  - 新增：12 个文件（多张图片 + `ppt-ai-consulting-plan.md`）
- 成功推送到 `origin/main`
- Cron 任务存在问题：模型 `lmstudio/qwen3.6-27b-q4km` 不在白名单中，飞书推送缺少目标配置
- 尽管 cron 配置有问题，同步仍顺利完成

## 语言偏好（22:33）
- 杰斯明确要求：所有会话一律使用中文，不许发英文
- 后续所有回复、记忆、提交信息均使用中文

## GitHub 同步持续更新策略（22:36）
- 杰斯要求每日 GitHub 同步保持每天更新
- 如果没按时打开 OpenClaw，打开后立即开始更新 GitHub
- 已重新创建 cron 任务（daily-github-sync），每天凌晨 3:00 执行，使用默认模型
- 同步逻辑：检查 git status → 有变更则 add/add images/ + commit + push → 无变更跳过

## 默认图片尺寸变更（22:47）
- 杰斯要求以后生成的图片默认都是 1080P（1920×1080，16:9 横屏）
- 已更新 image-generation skill，默认 size 从 1024x1024 改为 1920x1080
- 测试生成了一张治愈风格图片：小猫窗台暖阳水彩风
- 路径：`D:\openclaw-stack\workspace\images\image-1---5032a465-fd0a-4583-8fea-d74a6a497730.png`

## 新智元新闻分享（23:42）
- 杰斯分享了新智元的微信公众号文章：https://mp.weixin.qq.com/s/velyGejDRabGfRjrNUTcZQ
- 标题：《奥特曼亲封GPT-5.5「自闭天才」！原始智能拉开断层级差距，跑分碾压，token 省到飞起》
- 要点：GPT-5.5 性能强但"人味儿"不足；Bold Metrics 团队从 Claude 切换到 Codex 每月省 $32k；Codex 单周下载 9000 万；OpenAI 把 "Ask ChatGPT" 改为 "Message ChatGPT"
- 杰斯说"加入这个账号的新闻"，意图未明确（可能是订阅、存档、或定时关注），待后续确认

## 新智元加入日报（23:44）
- 杰斯明确要求把"新智元"账号的新闻加入每日 AI 重大新闻整理日报
- 日报数据源新增：新智元（微信公众号，AI 领域资讯）
- 日报 cron 任务（daily-ai-news-digest）需在生成日报时纳入新智元的最新文章
